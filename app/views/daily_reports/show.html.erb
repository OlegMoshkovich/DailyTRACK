<h1>Daily REPORT</h1>

<strong>
<p>Daily REPORT includes summary of all daily activities.
  <br>
</p>


<p><strong><%= link_to 'BACK',daily_reports_path %><strong></p>

<h3>NARRATIVE</h3>
<table>
  <tr> 
    <th helvetica="3"></th>
  </tr>
<% @daily_report.narratives.each do |narrative| %>
  <tr>
    <th><%= narrative.narrative %></th>
     <td><%= link_to '+', [narrative.daily_report, narrative],
               method: :delete,
               data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<%= stylesheet_link_tag "http://ajax.googleapis.com/ajax/libs/jqueryui/1.7.2/themes/redmond/jquery-ui.css", "application" %>
<%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js", "http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.1/jquery-ui.min.js", "application" %>



<h3>SUMMARY</h3>

<table>
  <tr>
    <th>HEADING</th>
    <th>ELEMENT</th>
    <th>START</th>
    <th>FINISH</th>
    <th>OPERATION</th>
    <th helvetica="3"></th>
  </tr>
<% @walls.each do |wall| %>
  <tr>
    <td><%= wall.heading %></td>
    <td><%= wall.element %></td>
    <td><%= wall.start %></td>
    <td><%= wall.finish %></td>
    <td><%= wall.operation %></td>
    <td><%= image_tag wall.image.url(:thumb) %></td>
  </tr>
   <% end -%>
   <% @slabs.each do |slab| %>
  <tr>
    <td><%= slab.heading %></td>
    <td><%= slab.element %></td>
    <td><%= slab.start %></td>
    <td><%= slab.finish %></td>
    <td><%= slab.operation %></td>
     <td><%= image_tag slab.image.url(:thumb) %></td>
  </tr>
  <% end -%>
  </table>
<% @walls.each do |wall| %>
  <tr>
    <td><%= image_tag wall.image.url(:medium) %></td>
  </tr>
   <% end -%>

   <% @slabs.each do |slab| %>
  <tr>
    <td><%= image_tag slab.image.url(:medium) %></td>
  </tr>
   <% end -%>


<h5>
<%= link_to "Export EXCEL",daily_report_path(format: "xls") %>
<br>
<%= link_to 'Daily REPORTS', daily_reports_path%>
<br>	
<%= link_to 'Inspection REPORTS', inspection_reports_path%>
<br>
<%= link_to 'PROJECT CONTROLLER', root_path%>
</h5>
